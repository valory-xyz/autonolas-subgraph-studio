{
  "name": "autonolas-tokenomics-subgraph",
  "scripts": {
    "codegen": "graph codegen subgraph.gnosis.yaml",
    "build": "graph build subgraph.gnosis.yaml",
    "generate-manifests": "node ../../scripts/generate-manifests.js --path=.",
    "deploy-arbitrum": "graph deploy --network arbitrum-one olas-arbitrum-staking subgraph.arbitrum-one.yaml",
    "deploy-base": "graph deploy --network base olas-base-staking subgraph.base.yaml",
    "deploy-celo": "graph deploy --network celo olas-celo-staking subgraph.celo.yaml",
    "deploy-gnosis": "graph deploy --network gnosis olas-gnosis-staking subgraph.gnosis.yaml",
    "deploy-ethereum": "graph deploy --network mainnet olas-ethereum-staking subgraph.mainnet.yaml",
    "deploy-polygon": "graph deploy --network matic olas-polygon-staking subgraph.matic.yaml",
    "deploy-optimism": "graph deploy --network optimism olas-optimism-staking subgraph.optimism.yaml",
    "test": "ln -s subgraph.gnosis.yaml subgraph.yaml && graph test; rm -f subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  },
  "resolutions": {
    "//": "Selective version resolutions. Two scoping forms in Yarn 1: (1) unscoped \"pkg\": \"^X\" forces ONE version everywhere — safe only for single-major packages. (2) Path-scoped \"**/parent/pkg\": \"^X\" forces pkg only when nested under parent — required for multi-major (e.g. picomatch 2.x+4.x, glob 7.x+11.x). The **/ prefix is mandatory; bare \"parent/pkg\" is silently ignored by Yarn 1 for transitively-nested instances. Each allowlist removal must be paired with a resolution (or a fresh-lockfile cascade). Workflow: edit package.json, rm yarn.lock + subgraphs/*/yarn.lock, yarn install in root + each subgraph, node scripts/audit-install-hooks.mjs --update, node scripts/audit.mjs — any \"no longer in production tree\" warnings name audit-allowlist entries that can now be removed.",
    "immutable": "^5.1.5",
    "cross-spawn": "^7.0.6",
    "semver": "^7.5.2",
    "lodash": "^4.18.0",
    "js-yaml": "^4.2.0",
    "follow-redirects": "^1.16.0",
    "ejs": "^3.1.10",
    "tmp": "^0.2.6",
    "undici": "^7.28.0",
    "apisauce": "^3.2.2",
    "form-data": "^4.0.6",
    "uuid": "^11.1.1",
    "**/micromatch/picomatch": "2.3.2",
    "**/@graphprotocol/graph-cli/glob": "^11.1.0",
    "**/@graphprotocol/graph-cli/yaml": "^2.8.3"
  }
}
